numpy>=2.1,<3
pandas>=2.2,<3
scipy>=1.14,<2
scikit-learn>=1.6,<2
shap>=0.51,<0.52
matplotlib>=3.9,<4
tqdm>=4.66,<5

# Install PyTorch with the CUDA wheel from the official PyTorch index.
# Verified on this Windows 11 + RTX 3060 setup:
# python -m pip install --no-cache-dir torch==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
